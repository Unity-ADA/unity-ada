
export type curators_id = |
  "ashe" | "EXAMPLE_NAME";

export type type = |
  'Token' | 'cNFT' | 'Pool' | 'App';

/** @theme */
export type size_type = 'xs' | 'sm' | 'md' | 'lg' | 'xl';

export type bold_type = 'medium' | 'bold';

/** @note @todo currently only implemented within ColouredChip, this needs to be extended */
export type color_type = |
  'violet' | 'sky' | 'amber' | 'rose' | 'neutral';
